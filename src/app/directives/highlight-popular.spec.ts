import { HighlightPopular } from './highlight-popular';

describe('HighlightPopular', () => {
  it('should create an instance', () => {
    const directive = new HighlightPopular();
    expect(directive).toBeTruthy();
  });
});
